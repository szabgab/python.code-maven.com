# A simple function - use as it is

This is just a simple function. We can call it. It is just an example.

{% embed include file="src/examples/decorators/no_wrapper.py" %}

The output looks simple:

```
myfunc started
myfunc ended
```

