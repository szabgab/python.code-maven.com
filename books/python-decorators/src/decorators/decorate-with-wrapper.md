# Use wrapper as a decorator

We can get the same result by putting `@wrap` as a decorator above the definition of our function.

{% embed include file="src/examples/decorators/decorate_with_wrapper.py" %}
