# Jupyter add

* Open an existing notebook: `add.ipynb`

Before commiting a Jupyter notebook in git, it is usually a good idea to remove all the generated content.
So:

```
Edit / Clear Output of all Cells
```

