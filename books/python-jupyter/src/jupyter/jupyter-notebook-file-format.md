# Jupyter Notebook file format

* Jupyter notebooks are JSON file with a `.ipynb` extension. Open one such file with a plain text editor.
* We can download file as Python (.py)  exporting the code as a stand-alone python program.


