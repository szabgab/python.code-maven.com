TODO

First show the mymath module and maybe also the mycalc module that has an error.
Show how to use them and how to manually test them.
Maybe also show a few other modules we need to test e.g. one with random numbers, one with timeout etc.



doctest / unittest show test failure as well
